# NEXT PROMPTS — C940 (refreshed 2026-08-03)

Highest leverage first. Copy one block per session.

---

## 1) Disk reclaim (YELLOW→GREEN path)

```
You are C940 ops. Disk is ~53GB free (YELLOW; floor 50; target 80).
Run dual-control reclaim: measure → list rebuildable leaves only → reclaim idle caches with before/after receipt.
No active agent kill. No raw-delete of repos. Update MACHINE-ESTATE / plans receipt.
```

## 2) Packet 6 dirty steward (prod no-ship)

```
Classify dirty trees for frankx.ai-vercel-website (~434, orphaned content-integrity-gate) and FrankX (~128, main ahead/behind).
Output recommended commit names only. enhance-never-erase. No reset --hard, no mass delete, no deploy.
Write fleet/reports/packet6-dirty-YYYY-MM-DD.md
```

## 3) PR merge queen (classify only)

```
Load starlight-queen-swarm + estate-pr-review-swarm.
Classify open non-draft PRs on frankx.ai-vercel-website and FrankX as NOW/NEXT/HOLD/CLOSE-REVIEW.
Independent acceptance required. No bulk merge. Update hermes plans matrix + kanban pr-merge-queen board.
```

## 4) R1 FrankX→GenCreator conversion decision

```
R1 is YELLOW: external gencreator.ai CTAs exist; primary nav still on-site /gencreator.
Produce a decision packet for primary chrome CTA (Professional register). No ship into dirty vercel WIP.
Evidence: live HTTP + file counts + Footer/nav paths.
```

## 5) Book Packet 4 (run ON Yoga Book)

```
You are Yoga Book frontend node. Clone frankxai/agentic-ops-hub → agentic-ops.
python scripts/fleet_inventory.py --machine yoga-book
python scripts/fleet_sync.py --machine yoga-book
Claim frontend lanes only. Write self heartbeat only. Report inventory to OPS-LEDGER / Swarm one-liner.
```

## 6) Music IS commit readiness (operator ask to commit)

```
In agentic-music-producer-os: tests already 26/26; skills installed.
Dry-run install; prepare commit message for untracked skills/docs/scripts WITHOUT secrets/.env/runs.
Do not push unless operator asks.
```

## 7) Railway estate follow-up

```
Read FrankX/docs/ops/RAILWAY-SWARM-COORDINATION-REVIEW-2026-08-03.md.
Estate not green: ClickHouse volume pressure + failed services.
Documentation-only unless operator authorizes deploy/restart/billing changes.
```

End.
