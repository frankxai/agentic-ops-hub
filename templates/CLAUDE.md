# CLAUDE.md

@AGENTS.md

## Claude Code Specifics

Everything shared lives in `AGENTS.md` (the cross-agent source of truth). Add ONLY Claude-specific behavior below — plan-mode preferences, subagent usage, hook expectations, MCP notes.

* Prefer plan mode for multi-file or architectural changes.
* Use subagents (Explore/Plan) for broad codebase searches instead of manual grepping.
* Skills in `.claude/skills/` auto-activate — `coding-guardrails` is generated by the sync script; do not edit it directly.
