# ACOS nightly health report — 2026-07-17 N3

Repository: `C:\Users\frank\agentic-creator-os`
Branch: `night/2026-07-17-acos-health`

## Result

Health and build checks pass after two low-risk fixes.

- Replaced POSIX-only `NODE_OPTIONS='--max-old-space-size=4096'` workspace
  typecheck commands with cross-shell `tsc --noEmit` commands in the creator,
  database, and evaluator MCP packages.
- Updated the observatory catalog test's stale skill threshold from `>20` to
  `>=15`. The generated catalog contains 19 canonical skills, while the
  repository health inventory reports 101 skills across all surfaces.
- No public API names changed. Generated build artifacts were not committed.

## Validation

All passed on Node 22.13.0 after `npm ci`:

- `npm run lint` — 0 errors, 0 warnings
- `node scripts/generate-stats.mjs --check`
- `npm run typecheck:all` — all 7 MCP workspaces
- `npm run build:all` — all 7 MCP workspaces
- `npm run observatory:test` — 6/6 passing
- `git diff --check`

## Dependency audit follow-up

`npm audit` reports 3 known vulnerabilities: 2 high and 1 moderate. Affected
packages are Nodemailer, Hono, and esbuild. The available Nodemailer fix is a
major-version update, so no dependency upgrade was made during this low-risk
health pass.
