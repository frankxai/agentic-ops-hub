# Learning Note: Agent Workbench OS

Run update: 2026-06-29 daily carousel factory pass.

## Learning Input

Official source signal:

- OpenAI and Anthropic official pages support conservative framing that coding
  agents are becoming more capable across codebases, tools, and environments.
- No model ranking, benchmark, or unsupported product capability claim is used.
- 2026-06-27 recheck kept the source posture conservative: Codex is framed as a
  coding agent that can read, edit, run code, and work in cloud/IDE/CLI contexts
  where configured; Claude Code is framed as an agentic coding tool that reads
  codebases, edits files, runs commands, and integrates with development tools.
- 2026-06-28 recheck keeps the same conservative posture. No benchmark, ranking,
  model-comparison, platform-performance, or autonomous-public-action claim is
  added.
- 2026-06-29 recheck keeps the same conservative posture. Official OpenAI Codex,
  OpenAI Codex cloud, and Anthropic Claude Code sources support product-context
  framing only; no benchmark, ranking, model-comparison, platform-performance,
  or autonomous-public-action claim is added.

Approval/rejection signal:

- `#social-approvals` still has queue-hygiene guidance: do not add noise while
  waiting items are undecided.
- Frank rejected the old visual bar. The old Agentic Coding OS is now
  `REBUILD_REQUIRED`, not a model.
- 2026-06-27 `#social-command` says the current approval item remains revised
  Agentic Coding OS and a decision is still needed before adding another live
  candidate. Therefore this run treats Agent Workbench OS as a replacement
  decision, not a net-new queue item.
- 2026-06-28 `#social-command` confirms Agent Workbench OS remains the active
  replacement decision item. Do not create another approval candidate until the
  current item is approved, edited, revised, or held.
- 2026-06-29 `#social-approvals` still has no explicit human decision, and
  `#social-command` again confirms Agent Workbench OS remains the active
  decision item. Keep queue hygiene: no new approval candidate and no duplicate
  approval request.

Design QA signal:

- Generated images are useful only for covers and metaphors.
- Exact text and diagrams need deterministic HTML/SVG/Figma/Canva.
- Prior generated cover was not campaign-defining; deterministic graphic system
  is selected for this rebuild.
- Contact sheet and cover preview were inspected again on 2026-06-27. The
  sequence remains coherent, readable, and free of generated text.
- Contact sheet and cover preview were inspected again on 2026-06-28. The cover
  has a clear first read, all 10 slides are present, and no obvious clipping,
  blank media, fake logos, or generated-image text were found.
- Contact sheet and cover preview were inspected again on 2026-06-29. The cover
  keeps a strong first read, the 10-slide contact sheet is complete, and no
  visible clipping, fake UI/logo, private data, or generated-image text was
  found. The deck remains replacement-quality rather than flagship because the
  cover is deterministic editorial design, not campaign-defining visual art.

Performance signal:

- No published-post metrics were available for this topic.
- No 2026-06-27 published-post metrics were found in the checked Slack signals.
- No 2026-06-28 published-post metrics were found in the checked Slack signals.
- No 2026-06-29 published-post metrics were found in the checked Slack or local
  pack signals.

Brand guide signal:

- FrankX should be direct, commercial, proof-aware, and mechanism-first.
- AI-Architect language should preserve governance and approval boundaries.

## Decision

Make:

- A single Agent Workbench carousel with exported visual assets.
- A replacement approval request for the existing queue, not a new unrelated
  candidate.
- A concise workroom refresh note, not a duplicate approval request.
- 2026-06-28: maintain the existing Agent Workbench candidate instead of adding
  a new approval item while the replacement decision is still waiting.
- 2026-06-29: maintain the existing Agent Workbench candidate and add only a
  learning/proof refresh while the replacement decision is still waiting.

Cut:

- Raw generated cover dependency.
- Slack-first framing.
- Approval-ready language below the 90/100 gate.
- 2026-06-28: new topical AI-lab claims that would raise claim risk without
  materially improving the reusable operating packet.
- 2026-06-29: duplicate approval posts and new candidate manufacturing while the
  live decision item is unresolved.

Carry forward:

- Intake -> workspace -> truth -> proof -> approval -> learning.
- Humans approve irreversible moves.
- Workroom proof can exist without approval routing.
- Approval routing should be explicit about replacing or holding the current
  waiting item.
- Queue hygiene: workroom proof is useful; repeated approval requests without a
  human decision are noise.
- 2026-06-28: use Agent Workbench OS as the active queue item until Frank replies
  `APPROVE`, `APPROVE WITH EDITS`, `REVISE`, or `HOLD`.

## 2026-06-29 Daily Factory Refresh

Learning input:

- official source signal: OpenAI Codex cloud and Anthropic Claude Code official
  docs still support conservative product-context framing only. No benchmark,
  model ranking, autonomous-public-action, deployment, spending, customer
  message, access-change, or Hermes/runtime activation claim was added.
- approval/rejection signal: `#social-approvals` still has no explicit human
  decision on Agent Workbench OS. `#social-command` again names it as the active
  approval decision and recommends LinkedIn PDF first, Instagram sequence
  second.
- design QA signal: cover preview and contact sheet were inspected again; all 10
  slides are visible, deterministic, and free of obvious clipping, fake logos,
  blank media, or generated-image text.
- performance signal: no published-post metrics found in checked local files or
  Slack signals.
- brand guide signal: keep FrankX direct and mechanism-first; keep AI-Architect
  governance and approval boundaries explicit.

Decision:

- make: a dated learning/proof refresh on the existing Agent Workbench OS pack.
- cut: duplicate approval posts and new low-confidence topical candidates while
  the queue is blocked.
- carry forward: visual QA 27/30, social-confidence 91/100, medium-low claim
  risk, and the same explicit approval gate: `APPROVE`, `APPROVE WITH EDITS`,
  `REVISE`, or `HOLD`.

## 2026-06-29 Daily Factory Refresh

Learning input:

- Official source signal: no new topical claim was added; keep only
  conservative official OpenAI Codex and Anthropic Claude Code context.
- Approval/rejection signal: live Slack check found no explicit human decision
  on Agent Workbench OS; `#social-command` still names it as the active approval
  decision item.
- Design QA signal: `cover-preview.png` and `contact-sheet.png` were inspected
  again; all 10 slides and required exports remain present.
- Performance signal: no published-post metrics available because the candidate
  remains undecided.
- Brand guide signal: keep FrankX / Builder Education mechanism-first and
  AI-Architect governance-aware.

Decision:

- Make: maintain Agent Workbench OS as the single active replacement candidate.
- Cut: no new carousel candidates or duplicate top-level approval posts.
- Carry forward: 27/30 visual QA, 91/100 social-confidence, medium-low claim
  risk, LinkedIn PDF primary, Instagram sequence secondary, explicit human
  approval gate.
- 2026-06-29: use Agent Workbench OS as the active queue item until Frank replies
  `APPROVE`, `APPROVE WITH EDITS`, `REVISE`, or `HOLD`.
