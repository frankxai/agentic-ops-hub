# Daily Factory Note - 2026-06-29

Status: no new carousel candidate created. `Agent Workbench OS` remains the
active replacement decision item for the older Agentic Coding OS direction.

## Learning Input

Official source signal:

- OpenAI Codex cloud docs still support only conservative wording: delegate
  coding work to Codex in a cloud environment.
- Anthropic Claude Code official overview still frames Claude Code as an
  agentic coding tool that reads codebases, edits files, runs commands, and
  integrates with development tools.
- No benchmark, ranking, model-comparison, autonomous publishing, deployment,
  customer-message, spend, access-change, or Hermes/runtime activation claim was
  added.

Approval/rejection signal:

- `#social-approvals` still has no explicit `APPROVE`, `APPROVE WITH EDITS`,
  `REVISE`, or `HOLD` decision on the Agent Workbench OS replacement request.
- `#social-command` on 2026-06-29 again says Agent Workbench OS remains the
  current approval decision and should stay LinkedIn PDF first, Instagram
  sequence second.
- Do not add another live approval candidate until this item moves.

Design QA signal:

- `cover-preview.png` and `contact-sheet.png` were inspected again.
- The cover keeps a strong first read, deterministic title, and clear workbench
  motif.
- The contact sheet shows all 10 slides, no obvious clipping, no blank media, no
  fake logos, and no generated-image text.
- Visual QA remains 27/30 and social-confidence remains 91/100.

Performance signal:

- No published-post metrics were found in the checked local files or Slack
  signals.
- Do not optimize a new variant without a publish decision or performance data.

Brand guide signal:

- FrankX / Builder Education guidance still favors direct, mechanism-first,
  proof-aware teaching.
- AI-Architect framing still needs explicit governance and human approval
  boundaries.

## Decision

Make:

- Keep Agent Workbench OS as the single active carousel approval item.
- Post only a workroom learning/proof refresh to `#social-carousels`.
- Keep the next potential lanes as prep only: Claim-Risk Ledger, Canon Before
  Campaign, Honest Recommendation Desk.

Cut:

- Duplicate top-level `#social-approvals` requests.
- New topical AI-lab claims that do not materially improve this packet.
- Any language implying publish, schedule, deploy, merge, spend, customer
  message, access change, domain action, or runtime activation is approved.

Carry forward:

- The active human decision remains `APPROVE`, `APPROVE WITH EDITS`, `REVISE`,
  or `HOLD`.
- LinkedIn uses `deck.pdf`; Instagram uses `exports/png/` or `exports/jpg/`.
- Exact slide text remains deterministic HTML/CSS/SVG.

## Artifact Check

- `deck.pdf`: present
- `index.html`: present
- `cover-preview.png`: present and inspected
- `contact-sheet.png`: present and inspected
- PNG sequence: 10 slides present
- JPG sequence: 10 slides present
- Placeholder scan: no actionable deck placeholder found in authored pack files

No public publishing, scheduling, customer or partner message, production
deployment, merge, spend, access change, domain action, or Hermes/runtime
activation was taken.
